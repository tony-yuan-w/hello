/*
 * Generated by gdbus-codegen 2.56.4. DO NOT EDIT.
 *
 * The license of this code is the same as for the D-Bus interface description
 * it was derived from.
 */

#ifdef HAVE_CONFIG_H
#  include "config.h"
#endif

#include "dbus_generate_server.h"

#include <string.h>
#ifdef G_OS_UNIX
#  include <gio/gunixfdlist.h>
#endif

typedef struct
{
  GDBusArgInfo parent_struct;
  gboolean use_gvariant;
} _ExtendedGDBusArgInfo;

typedef struct
{
  GDBusMethodInfo parent_struct;
  const gchar *signal_name;
  gboolean pass_fdlist;
} _ExtendedGDBusMethodInfo;

typedef struct
{
  GDBusSignalInfo parent_struct;
  const gchar *signal_name;
} _ExtendedGDBusSignalInfo;

typedef struct
{
  GDBusPropertyInfo parent_struct;
  const gchar *hyphen_name;
  gboolean use_gvariant;
} _ExtendedGDBusPropertyInfo;

typedef struct
{
  GDBusInterfaceInfo parent_struct;
  const gchar *hyphen_name;
} _ExtendedGDBusInterfaceInfo;

typedef struct
{
  const _ExtendedGDBusPropertyInfo *info;
  guint prop_id;
  GValue orig_value; /* the value before the change */
} ChangedProperty;

static void
_changed_property_free (ChangedProperty *data)
{
  g_value_unset (&data->orig_value);
  g_free (data);
}

static gboolean
_g_strv_equal0 (gchar **a, gchar **b)
{
  gboolean ret = FALSE;
  guint n;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  if (g_strv_length (a) != g_strv_length (b))
    goto out;
  for (n = 0; a[n] != NULL; n++)
    if (g_strcmp0 (a[n], b[n]) != 0)
      goto out;
  ret = TRUE;
out:
  return ret;
}

static gboolean
_g_variant_equal0 (GVariant *a, GVariant *b)
{
  gboolean ret = FALSE;
  if (a == NULL && b == NULL)
    {
      ret = TRUE;
      goto out;
    }
  if (a == NULL || b == NULL)
    goto out;
  ret = g_variant_equal (a, b);
out:
  return ret;
}

G_GNUC_UNUSED static gboolean
_g_value_equal (const GValue *a, const GValue *b)
{
  gboolean ret = FALSE;
  g_assert (G_VALUE_TYPE (a) == G_VALUE_TYPE (b));
  switch (G_VALUE_TYPE (a))
    {
      case G_TYPE_BOOLEAN:
        ret = (g_value_get_boolean (a) == g_value_get_boolean (b));
        break;
      case G_TYPE_UCHAR:
        ret = (g_value_get_uchar (a) == g_value_get_uchar (b));
        break;
      case G_TYPE_INT:
        ret = (g_value_get_int (a) == g_value_get_int (b));
        break;
      case G_TYPE_UINT:
        ret = (g_value_get_uint (a) == g_value_get_uint (b));
        break;
      case G_TYPE_INT64:
        ret = (g_value_get_int64 (a) == g_value_get_int64 (b));
        break;
      case G_TYPE_UINT64:
        ret = (g_value_get_uint64 (a) == g_value_get_uint64 (b));
        break;
      case G_TYPE_DOUBLE:
        {
          /* Avoid -Wfloat-equal warnings by doing a direct bit compare */
          gdouble da = g_value_get_double (a);
          gdouble db = g_value_get_double (b);
          ret = memcmp (&da, &db, sizeof (gdouble)) == 0;
        }
        break;
      case G_TYPE_STRING:
        ret = (g_strcmp0 (g_value_get_string (a), g_value_get_string (b)) == 0);
        break;
      case G_TYPE_VARIANT:
        ret = _g_variant_equal0 (g_value_get_variant (a), g_value_get_variant (b));
        break;
      default:
        if (G_VALUE_TYPE (a) == G_TYPE_STRV)
          ret = _g_strv_equal0 (g_value_get_boxed (a), g_value_get_boxed (b));
        else
          g_critical ("_g_value_equal() does not handle type %s", g_type_name (G_VALUE_TYPE (a)));
        break;
    }
  return ret;
}

/* ------------------------------------------------------------------------
 * Code for interface org.philips.chargerMgr
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:OrgPhilipsChargerMgr
 * @title: OrgPhilipsChargerMgr
 * @short_description: Generated C code for the org.philips.chargerMgr D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-philips-chargerMgr.top_of_page">org.philips.chargerMgr</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.philips.chargerMgr ---- */

static const _ExtendedGDBusMethodInfo _org_philips_charger_mgr_method_info_stop_charging =
{
  {
    -1,
    (gchar *) "StopCharging",
    NULL,
    NULL,
    NULL
  },
  "handle-stop-charging",
  FALSE
};

static const _ExtendedGDBusMethodInfo _org_philips_charger_mgr_method_info_resume_charging =
{
  {
    -1,
    (gchar *) "ResumeCharging",
    NULL,
    NULL,
    NULL
  },
  "handle-resume-charging",
  FALSE
};

static const _ExtendedGDBusMethodInfo * const _org_philips_charger_mgr_method_info_pointers[] =
{
  &_org_philips_charger_mgr_method_info_stop_charging,
  &_org_philips_charger_mgr_method_info_resume_charging,
  NULL
};

static const _ExtendedGDBusArgInfo _org_philips_charger_mgr_signal_info_ac_change_ARG_state =
{
  {
    -1,
    (gchar *) "state",
    (gchar *) "b",
    NULL
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _org_philips_charger_mgr_signal_info_ac_change_ARG_pointers[] =
{
  &_org_philips_charger_mgr_signal_info_ac_change_ARG_state,
  NULL
};

static const _ExtendedGDBusSignalInfo _org_philips_charger_mgr_signal_info_ac_change =
{
  {
    -1,
    (gchar *) "AcChange",
    (GDBusArgInfo **) &_org_philips_charger_mgr_signal_info_ac_change_ARG_pointers,
    NULL
  },
  "ac-change"
};

static const _ExtendedGDBusSignalInfo * const _org_philips_charger_mgr_signal_info_pointers[] =
{
  &_org_philips_charger_mgr_signal_info_ac_change,
  NULL
};

static const _ExtendedGDBusPropertyInfo _org_philips_charger_mgr_property_info_ac_online =
{
  {
    -1,
    (gchar *) "AcOnline",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "ac-online",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_charger_mgr_property_info_test =
{
  {
    -1,
    (gchar *) "test",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "test",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _org_philips_charger_mgr_property_info_pointers[] =
{
  &_org_philips_charger_mgr_property_info_ac_online,
  &_org_philips_charger_mgr_property_info_test,
  NULL
};

static const GDBusAnnotationInfo _org_philips_charger_mgr_annotation_info_0 =
{
  -1,
  (gchar *) "org.philips.Annotation",
  (gchar *) "OnInterface",
  NULL
};

static const GDBusAnnotationInfo _org_philips_charger_mgr_annotation_info_1 =
{
  -1,
  (gchar *) "org.philips.Annotation",
  (gchar *) "AlsoOnInterface",
  NULL
};

static const GDBusAnnotationInfo * const _org_philips_charger_mgr_annotation_info_pointers[] =
{
  &_org_philips_charger_mgr_annotation_info_0,
  &_org_philips_charger_mgr_annotation_info_1,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _org_philips_charger_mgr_interface_info =
{
  {
    -1,
    (gchar *) "org.philips.chargerMgr",
    (GDBusMethodInfo **) &_org_philips_charger_mgr_method_info_pointers,
    (GDBusSignalInfo **) &_org_philips_charger_mgr_signal_info_pointers,
    (GDBusPropertyInfo **) &_org_philips_charger_mgr_property_info_pointers,
    (GDBusAnnotationInfo **) &_org_philips_charger_mgr_annotation_info_pointers
  },
  "org-philips-charger-mgr",
};


/**
 * org_philips_charger_mgr_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-philips-chargerMgr.top_of_page">org.philips.chargerMgr</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
org_philips_charger_mgr_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_org_philips_charger_mgr_interface_info.parent_struct;
}

/**
 * org_philips_charger_mgr_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #OrgPhilipsChargerMgr interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
org_philips_charger_mgr_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "ac-online");
  g_object_class_override_property (klass, property_id_begin++, "test");
  return property_id_begin - 1;
}



/**
 * OrgPhilipsChargerMgr:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-philips-chargerMgr.top_of_page">org.philips.chargerMgr</link>.
 */

/**
 * OrgPhilipsChargerMgrIface:
 * @parent_iface: The parent interface.
 * @handle_resume_charging: Handler for the #OrgPhilipsChargerMgr::handle-resume-charging signal.
 * @handle_stop_charging: Handler for the #OrgPhilipsChargerMgr::handle-stop-charging signal.
 * @get_ac_online: Getter for the #OrgPhilipsChargerMgr:ac-online property.
 * @get_test: Getter for the #OrgPhilipsChargerMgr:test property.
 * @ac_change: Handler for the #OrgPhilipsChargerMgr::ac-change signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-philips-chargerMgr.top_of_page">org.philips.chargerMgr</link>.
 */

typedef OrgPhilipsChargerMgrIface OrgPhilipsChargerMgrInterface;
G_DEFINE_INTERFACE (OrgPhilipsChargerMgr, org_philips_charger_mgr, G_TYPE_OBJECT)

static void
org_philips_charger_mgr_default_init (OrgPhilipsChargerMgrIface *iface)
{
  /* GObject signals for incoming D-Bus method calls: */
  /**
   * OrgPhilipsChargerMgr::handle-stop-charging:
   * @object: A #OrgPhilipsChargerMgr.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-philips-chargerMgr.StopCharging">StopCharging()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call org_philips_charger_mgr_complete_stop_charging() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-stop-charging",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OrgPhilipsChargerMgrIface, handle_stop_charging),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /**
   * OrgPhilipsChargerMgr::handle-resume-charging:
   * @object: A #OrgPhilipsChargerMgr.
   * @invocation: A #GDBusMethodInvocation.
   *
   * Signal emitted when a remote caller is invoking the <link linkend="gdbus-method-org-philips-chargerMgr.ResumeCharging">ResumeCharging()</link> D-Bus method.
   *
   * If a signal handler returns %TRUE, it means the signal handler will handle the invocation (e.g. take a reference to @invocation and eventually call org_philips_charger_mgr_complete_resume_charging() or e.g. g_dbus_method_invocation_return_error() on it) and no order signal handlers will run. If no signal handler handles the invocation, the %G_DBUS_ERROR_UNKNOWN_METHOD error is returned.
   *
   * Returns: %TRUE if the invocation was handled, %FALSE to let other signal handlers run.
   */
  g_signal_new ("handle-resume-charging",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OrgPhilipsChargerMgrIface, handle_resume_charging),
    g_signal_accumulator_true_handled,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_BOOLEAN,
    1,
    G_TYPE_DBUS_METHOD_INVOCATION);

  /* GObject signals for received D-Bus signals: */
  /**
   * OrgPhilipsChargerMgr::ac-change:
   * @object: A #OrgPhilipsChargerMgr.
   * @arg_state: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-philips-chargerMgr.AcChange">"AcChange"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("ac-change",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OrgPhilipsChargerMgrIface, ac_change),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_BOOLEAN);

  /* GObject properties for D-Bus properties: */
  /**
   * OrgPhilipsChargerMgr:ac-online:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-chargerMgr.AcOnline">"AcOnline"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("ac-online", "AcOnline", "AcOnline", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsChargerMgr:test:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-chargerMgr.test">"test"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("test", "test", "test", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * org_philips_charger_mgr_get_ac_online: (skip)
 * @object: A #OrgPhilipsChargerMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-chargerMgr.AcOnline">"AcOnline"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
org_philips_charger_mgr_get_ac_online (OrgPhilipsChargerMgr *object)
{
  return ORG_PHILIPS_CHARGER_MGR_GET_IFACE (object)->get_ac_online (object);
}

/**
 * org_philips_charger_mgr_set_ac_online: (skip)
 * @object: A #OrgPhilipsChargerMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-chargerMgr.AcOnline">"AcOnline"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_charger_mgr_set_ac_online (OrgPhilipsChargerMgr *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "ac-online", value, NULL);
}

/**
 * org_philips_charger_mgr_get_test: (skip)
 * @object: A #OrgPhilipsChargerMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-chargerMgr.test">"test"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
org_philips_charger_mgr_get_test (OrgPhilipsChargerMgr *object)
{
  return ORG_PHILIPS_CHARGER_MGR_GET_IFACE (object)->get_test (object);
}

/**
 * org_philips_charger_mgr_set_test: (skip)
 * @object: A #OrgPhilipsChargerMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-chargerMgr.test">"test"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_charger_mgr_set_test (OrgPhilipsChargerMgr *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "test", value, NULL);
}

/**
 * org_philips_charger_mgr_emit_ac_change:
 * @object: A #OrgPhilipsChargerMgr.
 * @arg_state: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-philips-chargerMgr.AcChange">"AcChange"</link> D-Bus signal.
 */
void
org_philips_charger_mgr_emit_ac_change (
    OrgPhilipsChargerMgr *object,
    gboolean arg_state)
{
  g_signal_emit_by_name (object, "ac-change", arg_state);
}

/**
 * org_philips_charger_mgr_call_stop_charging:
 * @proxy: A #OrgPhilipsChargerMgrProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-philips-chargerMgr.StopCharging">StopCharging()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call org_philips_charger_mgr_call_stop_charging_finish() to get the result of the operation.
 *
 * See org_philips_charger_mgr_call_stop_charging_sync() for the synchronous, blocking version of this method.
 */
void
org_philips_charger_mgr_call_stop_charging (
    OrgPhilipsChargerMgr *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "StopCharging",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * org_philips_charger_mgr_call_stop_charging_finish:
 * @proxy: A #OrgPhilipsChargerMgrProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_philips_charger_mgr_call_stop_charging().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with org_philips_charger_mgr_call_stop_charging().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
org_philips_charger_mgr_call_stop_charging_finish (
    OrgPhilipsChargerMgr *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_philips_charger_mgr_call_stop_charging_sync:
 * @proxy: A #OrgPhilipsChargerMgrProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-philips-chargerMgr.StopCharging">StopCharging()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See org_philips_charger_mgr_call_stop_charging() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
org_philips_charger_mgr_call_stop_charging_sync (
    OrgPhilipsChargerMgr *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "StopCharging",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_philips_charger_mgr_call_resume_charging:
 * @proxy: A #OrgPhilipsChargerMgrProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied or %NULL.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously invokes the <link linkend="gdbus-method-org-philips-chargerMgr.ResumeCharging">ResumeCharging()</link> D-Bus method on @proxy.
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call org_philips_charger_mgr_call_resume_charging_finish() to get the result of the operation.
 *
 * See org_philips_charger_mgr_call_resume_charging_sync() for the synchronous, blocking version of this method.
 */
void
org_philips_charger_mgr_call_resume_charging (
    OrgPhilipsChargerMgr *proxy,
    GCancellable *cancellable,
    GAsyncReadyCallback callback,
    gpointer user_data)
{
  g_dbus_proxy_call (G_DBUS_PROXY (proxy),
    "ResumeCharging",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    callback,
    user_data);
}

/**
 * org_philips_charger_mgr_call_resume_charging_finish:
 * @proxy: A #OrgPhilipsChargerMgrProxy.
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_philips_charger_mgr_call_resume_charging().
 * @error: Return location for error or %NULL.
 *
 * Finishes an operation started with org_philips_charger_mgr_call_resume_charging().
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
org_philips_charger_mgr_call_resume_charging_finish (
    OrgPhilipsChargerMgr *proxy,
    GAsyncResult *res,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_finish (G_DBUS_PROXY (proxy), res, error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_philips_charger_mgr_call_resume_charging_sync:
 * @proxy: A #OrgPhilipsChargerMgrProxy.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL.
 *
 * Synchronously invokes the <link linkend="gdbus-method-org-philips-chargerMgr.ResumeCharging">ResumeCharging()</link> D-Bus method on @proxy. The calling thread is blocked until a reply is received.
 *
 * See org_philips_charger_mgr_call_resume_charging() for the asynchronous version of this method.
 *
 * Returns: (skip): %TRUE if the call succeded, %FALSE if @error is set.
 */
gboolean
org_philips_charger_mgr_call_resume_charging_sync (
    OrgPhilipsChargerMgr *proxy,
    GCancellable *cancellable,
    GError **error)
{
  GVariant *_ret;
  _ret = g_dbus_proxy_call_sync (G_DBUS_PROXY (proxy),
    "ResumeCharging",
    g_variant_new ("()"),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    cancellable,
    error);
  if (_ret == NULL)
    goto _out;
  g_variant_get (_ret,
                 "()");
  g_variant_unref (_ret);
_out:
  return _ret != NULL;
}

/**
 * org_philips_charger_mgr_complete_stop_charging:
 * @object: A #OrgPhilipsChargerMgr.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-philips-chargerMgr.StopCharging">StopCharging()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
org_philips_charger_mgr_complete_stop_charging (
    OrgPhilipsChargerMgr *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/**
 * org_philips_charger_mgr_complete_resume_charging:
 * @object: A #OrgPhilipsChargerMgr.
 * @invocation: (transfer full): A #GDBusMethodInvocation.
 *
 * Helper function used in service implementations to finish handling invocations of the <link linkend="gdbus-method-org-philips-chargerMgr.ResumeCharging">ResumeCharging()</link> D-Bus method. If you instead want to finish handling an invocation by returning an error, use g_dbus_method_invocation_return_error() or similar.
 *
 * This method will free @invocation, you cannot use it afterwards.
 */
void
org_philips_charger_mgr_complete_resume_charging (
    OrgPhilipsChargerMgr *object,
    GDBusMethodInvocation *invocation)
{
  g_dbus_method_invocation_return_value (invocation,
    g_variant_new ("()"));
}

/* ------------------------------------------------------------------------ */

/**
 * OrgPhilipsChargerMgrProxy:
 *
 * The #OrgPhilipsChargerMgrProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * OrgPhilipsChargerMgrProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #OrgPhilipsChargerMgrProxy.
 */

struct _OrgPhilipsChargerMgrProxyPrivate
{
  GData *qdata;
};

static void org_philips_charger_mgr_proxy_iface_init (OrgPhilipsChargerMgrIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (OrgPhilipsChargerMgrProxy, org_philips_charger_mgr_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (OrgPhilipsChargerMgrProxy)
                         G_IMPLEMENT_INTERFACE (TYPE_ORG_PHILIPS_CHARGER_MGR, org_philips_charger_mgr_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (OrgPhilipsChargerMgrProxy, org_philips_charger_mgr_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_ORG_PHILIPS_CHARGER_MGR, org_philips_charger_mgr_proxy_iface_init))

#endif
static void
org_philips_charger_mgr_proxy_finalize (GObject *object)
{
  OrgPhilipsChargerMgrProxy *proxy = ORG_PHILIPS_CHARGER_MGR_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (org_philips_charger_mgr_proxy_parent_class)->finalize (object);
}

static void
org_philips_charger_mgr_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  info = _org_philips_charger_mgr_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
org_philips_charger_mgr_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.philips.chargerMgr: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
org_philips_charger_mgr_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  info = _org_philips_charger_mgr_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.philips.chargerMgr", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) org_philips_charger_mgr_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
org_philips_charger_mgr_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_org_philips_charger_mgr_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_ORG_PHILIPS_CHARGER_MGR);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_ORG_PHILIPS_CHARGER_MGR);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
org_philips_charger_mgr_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  OrgPhilipsChargerMgrProxy *proxy = ORG_PHILIPS_CHARGER_MGR_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_org_philips_charger_mgr_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_org_philips_charger_mgr_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static gboolean 
org_philips_charger_mgr_proxy_get_ac_online (OrgPhilipsChargerMgr *object)
{
  OrgPhilipsChargerMgrProxy *proxy = ORG_PHILIPS_CHARGER_MGR_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "AcOnline");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
org_philips_charger_mgr_proxy_get_test (OrgPhilipsChargerMgr *object)
{
  OrgPhilipsChargerMgrProxy *proxy = ORG_PHILIPS_CHARGER_MGR_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "test");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static void
org_philips_charger_mgr_proxy_init (OrgPhilipsChargerMgrProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = org_philips_charger_mgr_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_ORG_PHILIPS_CHARGER_MGR_PROXY, OrgPhilipsChargerMgrProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), org_philips_charger_mgr_interface_info ());
}

static void
org_philips_charger_mgr_proxy_class_init (OrgPhilipsChargerMgrProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = org_philips_charger_mgr_proxy_finalize;
  gobject_class->get_property = org_philips_charger_mgr_proxy_get_property;
  gobject_class->set_property = org_philips_charger_mgr_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = org_philips_charger_mgr_proxy_g_signal;
  proxy_class->g_properties_changed = org_philips_charger_mgr_proxy_g_properties_changed;

  org_philips_charger_mgr_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (OrgPhilipsChargerMgrProxyPrivate));
#endif
}

static void
org_philips_charger_mgr_proxy_iface_init (OrgPhilipsChargerMgrIface *iface)
{
  iface->get_ac_online = org_philips_charger_mgr_proxy_get_ac_online;
  iface->get_test = org_philips_charger_mgr_proxy_get_test;
}

/**
 * org_philips_charger_mgr_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-philips-chargerMgr.top_of_page">org.philips.chargerMgr</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call org_philips_charger_mgr_proxy_new_finish() to get the result of the operation.
 *
 * See org_philips_charger_mgr_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
org_philips_charger_mgr_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_ORG_PHILIPS_CHARGER_MGR_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.philips.chargerMgr", NULL);
}

/**
 * org_philips_charger_mgr_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_philips_charger_mgr_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with org_philips_charger_mgr_proxy_new().
 *
 * Returns: (transfer full) (type OrgPhilipsChargerMgrProxy): The constructed proxy object or %NULL if @error is set.
 */
OrgPhilipsChargerMgr *
org_philips_charger_mgr_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return ORG_PHILIPS_CHARGER_MGR (ret);
  else
    return NULL;
}

/**
 * org_philips_charger_mgr_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-philips-chargerMgr.top_of_page">org.philips.chargerMgr</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See org_philips_charger_mgr_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type OrgPhilipsChargerMgrProxy): The constructed proxy object or %NULL if @error is set.
 */
OrgPhilipsChargerMgr *
org_philips_charger_mgr_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_ORG_PHILIPS_CHARGER_MGR_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.philips.chargerMgr", NULL);
  if (ret != NULL)
    return ORG_PHILIPS_CHARGER_MGR (ret);
  else
    return NULL;
}


/**
 * org_philips_charger_mgr_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like org_philips_charger_mgr_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call org_philips_charger_mgr_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See org_philips_charger_mgr_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
org_philips_charger_mgr_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_ORG_PHILIPS_CHARGER_MGR_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.philips.chargerMgr", NULL);
}

/**
 * org_philips_charger_mgr_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_philips_charger_mgr_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with org_philips_charger_mgr_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type OrgPhilipsChargerMgrProxy): The constructed proxy object or %NULL if @error is set.
 */
OrgPhilipsChargerMgr *
org_philips_charger_mgr_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return ORG_PHILIPS_CHARGER_MGR (ret);
  else
    return NULL;
}

/**
 * org_philips_charger_mgr_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like org_philips_charger_mgr_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See org_philips_charger_mgr_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type OrgPhilipsChargerMgrProxy): The constructed proxy object or %NULL if @error is set.
 */
OrgPhilipsChargerMgr *
org_philips_charger_mgr_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_ORG_PHILIPS_CHARGER_MGR_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.philips.chargerMgr", NULL);
  if (ret != NULL)
    return ORG_PHILIPS_CHARGER_MGR (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * OrgPhilipsChargerMgrSkeleton:
 *
 * The #OrgPhilipsChargerMgrSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * OrgPhilipsChargerMgrSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #OrgPhilipsChargerMgrSkeleton.
 */

struct _OrgPhilipsChargerMgrSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_org_philips_charger_mgr_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  OrgPhilipsChargerMgrSkeleton *skeleton = ORG_PHILIPS_CHARGER_MGR_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_ORG_PHILIPS_CHARGER_MGR);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_ORG_PHILIPS_CHARGER_MGR);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_org_philips_charger_mgr_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  OrgPhilipsChargerMgrSkeleton *skeleton = ORG_PHILIPS_CHARGER_MGR_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_org_philips_charger_mgr_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_org_philips_charger_mgr_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  OrgPhilipsChargerMgrSkeleton *skeleton = ORG_PHILIPS_CHARGER_MGR_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_org_philips_charger_mgr_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _org_philips_charger_mgr_skeleton_vtable =
{
  _org_philips_charger_mgr_skeleton_handle_method_call,
  _org_philips_charger_mgr_skeleton_handle_get_property,
  _org_philips_charger_mgr_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
org_philips_charger_mgr_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return org_philips_charger_mgr_interface_info ();
}

static GDBusInterfaceVTable *
org_philips_charger_mgr_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_org_philips_charger_mgr_skeleton_vtable;
}

static GVariant *
org_philips_charger_mgr_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  OrgPhilipsChargerMgrSkeleton *skeleton = ORG_PHILIPS_CHARGER_MGR_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_org_philips_charger_mgr_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _org_philips_charger_mgr_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _org_philips_charger_mgr_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _org_philips_charger_mgr_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.philips.chargerMgr", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _org_philips_charger_mgr_emit_changed (gpointer user_data);

static void
org_philips_charger_mgr_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  OrgPhilipsChargerMgrSkeleton *skeleton = ORG_PHILIPS_CHARGER_MGR_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _org_philips_charger_mgr_emit_changed (skeleton);
}

static void
_org_philips_charger_mgr_on_signal_ac_change (
    OrgPhilipsChargerMgr *object,
    gboolean arg_state)
{
  OrgPhilipsChargerMgrSkeleton *skeleton = ORG_PHILIPS_CHARGER_MGR_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(b)",
                   arg_state));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.philips.chargerMgr", "AcChange",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void org_philips_charger_mgr_skeleton_iface_init (OrgPhilipsChargerMgrIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (OrgPhilipsChargerMgrSkeleton, org_philips_charger_mgr_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (OrgPhilipsChargerMgrSkeleton)
                         G_IMPLEMENT_INTERFACE (TYPE_ORG_PHILIPS_CHARGER_MGR, org_philips_charger_mgr_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (OrgPhilipsChargerMgrSkeleton, org_philips_charger_mgr_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_ORG_PHILIPS_CHARGER_MGR, org_philips_charger_mgr_skeleton_iface_init))

#endif
static void
org_philips_charger_mgr_skeleton_finalize (GObject *object)
{
  OrgPhilipsChargerMgrSkeleton *skeleton = ORG_PHILIPS_CHARGER_MGR_SKELETON (object);
  guint n;
  for (n = 0; n < 2; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (org_philips_charger_mgr_skeleton_parent_class)->finalize (object);
}

static void
org_philips_charger_mgr_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  OrgPhilipsChargerMgrSkeleton *skeleton = ORG_PHILIPS_CHARGER_MGR_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_org_philips_charger_mgr_emit_changed (gpointer user_data)
{
  OrgPhilipsChargerMgrSkeleton *skeleton = ORG_PHILIPS_CHARGER_MGR_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.philips.chargerMgr",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_org_philips_charger_mgr_schedule_emit_changed (OrgPhilipsChargerMgrSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
org_philips_charger_mgr_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  OrgPhilipsChargerMgrSkeleton *skeleton = ORG_PHILIPS_CHARGER_MGR_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _org_philips_charger_mgr_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _org_philips_charger_mgr_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
org_philips_charger_mgr_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  OrgPhilipsChargerMgrSkeleton *skeleton = ORG_PHILIPS_CHARGER_MGR_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 2);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _org_philips_charger_mgr_schedule_emit_changed (skeleton, _org_philips_charger_mgr_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
org_philips_charger_mgr_skeleton_init (OrgPhilipsChargerMgrSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = org_philips_charger_mgr_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_ORG_PHILIPS_CHARGER_MGR_SKELETON, OrgPhilipsChargerMgrSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 2);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_BOOLEAN);
}

static gboolean 
org_philips_charger_mgr_skeleton_get_ac_online (OrgPhilipsChargerMgr *object)
{
  OrgPhilipsChargerMgrSkeleton *skeleton = ORG_PHILIPS_CHARGER_MGR_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
org_philips_charger_mgr_skeleton_get_test (OrgPhilipsChargerMgr *object)
{
  OrgPhilipsChargerMgrSkeleton *skeleton = ORG_PHILIPS_CHARGER_MGR_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
org_philips_charger_mgr_skeleton_class_init (OrgPhilipsChargerMgrSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = org_philips_charger_mgr_skeleton_finalize;
  gobject_class->get_property = org_philips_charger_mgr_skeleton_get_property;
  gobject_class->set_property = org_philips_charger_mgr_skeleton_set_property;
  gobject_class->notify       = org_philips_charger_mgr_skeleton_notify;


  org_philips_charger_mgr_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = org_philips_charger_mgr_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = org_philips_charger_mgr_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = org_philips_charger_mgr_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = org_philips_charger_mgr_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (OrgPhilipsChargerMgrSkeletonPrivate));
#endif
}

static void
org_philips_charger_mgr_skeleton_iface_init (OrgPhilipsChargerMgrIface *iface)
{
  iface->ac_change = _org_philips_charger_mgr_on_signal_ac_change;
  iface->get_ac_online = org_philips_charger_mgr_skeleton_get_ac_online;
  iface->get_test = org_philips_charger_mgr_skeleton_get_test;
}

/**
 * org_philips_charger_mgr_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-philips-chargerMgr.top_of_page">org.philips.chargerMgr</link>.
 *
 * Returns: (transfer full) (type OrgPhilipsChargerMgrSkeleton): The skeleton object.
 */
OrgPhilipsChargerMgr *
org_philips_charger_mgr_skeleton_new (void)
{
  return ORG_PHILIPS_CHARGER_MGR (g_object_new (TYPE_ORG_PHILIPS_CHARGER_MGR_SKELETON, NULL));
}

/* ------------------------------------------------------------------------
 * Code for interface org.philips.batteryMgr
 * ------------------------------------------------------------------------
 */

/**
 * SECTION:OrgPhilipsBatteryMgr
 * @title: OrgPhilipsBatteryMgr
 * @short_description: Generated C code for the org.philips.batteryMgr D-Bus interface
 *
 * This section contains code for working with the <link linkend="gdbus-interface-org-philips-batteryMgr.top_of_page">org.philips.batteryMgr</link> D-Bus interface in C.
 */

/* ---- Introspection data for org.philips.batteryMgr ---- */

static const GDBusAnnotationInfo _org_philips_battery_mgr_signal_info_battery_warn_ARG_arg_type_annotation_info_0 =
{
  -1,
  (gchar *) "org.philips.Annotation",
  (gchar *) "OnArg_NonFirst",
  NULL
};

static const GDBusAnnotationInfo * const _org_philips_battery_mgr_signal_info_battery_warn_ARG_arg_type_annotation_info_pointers[] =
{
  &_org_philips_battery_mgr_signal_info_battery_warn_ARG_arg_type_annotation_info_0,
  NULL
};

static const _ExtendedGDBusArgInfo _org_philips_battery_mgr_signal_info_battery_warn_ARG_type =
{
  {
    -1,
    (gchar *) "type",
    (gchar *) "u",
    (GDBusAnnotationInfo **) &_org_philips_battery_mgr_signal_info_battery_warn_ARG_arg_type_annotation_info_pointers
  },
  FALSE
};

static const _ExtendedGDBusArgInfo * const _org_philips_battery_mgr_signal_info_battery_warn_ARG_pointers[] =
{
  &_org_philips_battery_mgr_signal_info_battery_warn_ARG_type,
  NULL
};

static const GDBusAnnotationInfo _org_philips_battery_mgr_signal_battery_warn_annotation_info_0 =
{
  -1,
  (gchar *) "org.philips.Annotation",
  (gchar *) "Onsignal",
  NULL
};

static const GDBusAnnotationInfo * const _org_philips_battery_mgr_signal_battery_warn_annotation_info_pointers[] =
{
  &_org_philips_battery_mgr_signal_battery_warn_annotation_info_0,
  NULL
};

static const _ExtendedGDBusSignalInfo _org_philips_battery_mgr_signal_info_battery_warn =
{
  {
    -1,
    (gchar *) "BatteryWarn",
    (GDBusArgInfo **) &_org_philips_battery_mgr_signal_info_battery_warn_ARG_pointers,
    (GDBusAnnotationInfo **) &_org_philips_battery_mgr_signal_battery_warn_annotation_info_pointers
  },
  "battery-warn"
};

static const _ExtendedGDBusSignalInfo * const _org_philips_battery_mgr_signal_info_pointers[] =
{
  &_org_philips_battery_mgr_signal_info_battery_warn,
  NULL
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_qualified =
{
  {
    -1,
    (gchar *) "qualified",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "qualified",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_present =
{
  {
    -1,
    (gchar *) "present",
    (gchar *) "b",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "present",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_cycle_count =
{
  {
    -1,
    (gchar *) "cycle_count",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "cycle-count",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_status =
{
  {
    -1,
    (gchar *) "status",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "status",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_charge_now =
{
  {
    -1,
    (gchar *) "charge_now",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "charge-now",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_charge_full =
{
  {
    -1,
    (gchar *) "charge_full",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "charge-full",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_charge_full_design =
{
  {
    -1,
    (gchar *) "charge_full_design",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "charge-full-design",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_mode_register =
{
  {
    -1,
    (gchar *) "mode_register",
    (gchar *) "i",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "mode-register",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_status_register =
{
  {
    -1,
    (gchar *) "status_register",
    (gchar *) "i",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "status-register",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_capacity =
{
  {
    -1,
    (gchar *) "capacity",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "capacity",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_absolute_charge =
{
  {
    -1,
    (gchar *) "absolute_charge",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "absolute-charge",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_time_to_empty_avg =
{
  {
    -1,
    (gchar *) "time_to_empty_avg",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "time-to-empty-avg",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_time_to_full_avg =
{
  {
    -1,
    (gchar *) "time_to_full_avg",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "time-to-full-avg",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_max_error =
{
  {
    -1,
    (gchar *) "max_error",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "max-error",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_constant_charge_current =
{
  {
    -1,
    (gchar *) "constant_charge_current",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "constant-charge-current",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_constant_charge_voltage =
{
  {
    -1,
    (gchar *) "constant_charge_voltage",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "constant-charge-voltage",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_temp =
{
  {
    -1,
    (gchar *) "temp",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "temp",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_time_to_empty_now =
{
  {
    -1,
    (gchar *) "time_to_empty_now",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "time-to-empty-now",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_current_avg =
{
  {
    -1,
    (gchar *) "current_avg",
    (gchar *) "u",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "current-avg",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_date =
{
  {
    -1,
    (gchar *) "date",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "date",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_manufacturer =
{
  {
    -1,
    (gchar *) "manufacturer",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "manufacturer",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_model_name =
{
  {
    -1,
    (gchar *) "model_name",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "model-name",
  FALSE
};

static const _ExtendedGDBusPropertyInfo _org_philips_battery_mgr_property_info_serial_number =
{
  {
    -1,
    (gchar *) "serial_number",
    (gchar *) "s",
    G_DBUS_PROPERTY_INFO_FLAGS_READABLE,
    NULL
  },
  "serial-number",
  FALSE
};

static const _ExtendedGDBusPropertyInfo * const _org_philips_battery_mgr_property_info_pointers[] =
{
  &_org_philips_battery_mgr_property_info_qualified,
  &_org_philips_battery_mgr_property_info_present,
  &_org_philips_battery_mgr_property_info_cycle_count,
  &_org_philips_battery_mgr_property_info_status,
  &_org_philips_battery_mgr_property_info_charge_now,
  &_org_philips_battery_mgr_property_info_charge_full,
  &_org_philips_battery_mgr_property_info_charge_full_design,
  &_org_philips_battery_mgr_property_info_mode_register,
  &_org_philips_battery_mgr_property_info_status_register,
  &_org_philips_battery_mgr_property_info_capacity,
  &_org_philips_battery_mgr_property_info_absolute_charge,
  &_org_philips_battery_mgr_property_info_time_to_empty_avg,
  &_org_philips_battery_mgr_property_info_time_to_full_avg,
  &_org_philips_battery_mgr_property_info_max_error,
  &_org_philips_battery_mgr_property_info_constant_charge_current,
  &_org_philips_battery_mgr_property_info_constant_charge_voltage,
  &_org_philips_battery_mgr_property_info_temp,
  &_org_philips_battery_mgr_property_info_time_to_empty_now,
  &_org_philips_battery_mgr_property_info_current_avg,
  &_org_philips_battery_mgr_property_info_date,
  &_org_philips_battery_mgr_property_info_manufacturer,
  &_org_philips_battery_mgr_property_info_model_name,
  &_org_philips_battery_mgr_property_info_serial_number,
  NULL
};

static const GDBusAnnotationInfo _org_philips_battery_mgr_annotation_info_0 =
{
  -1,
  (gchar *) "org.philips.Annotation",
  (gchar *) "OnInterface",
  NULL
};

static const GDBusAnnotationInfo _org_philips_battery_mgr_annotation_info_1 =
{
  -1,
  (gchar *) "org.philips.Annotation",
  (gchar *) "AlsoOnInterface",
  NULL
};

static const GDBusAnnotationInfo * const _org_philips_battery_mgr_annotation_info_pointers[] =
{
  &_org_philips_battery_mgr_annotation_info_0,
  &_org_philips_battery_mgr_annotation_info_1,
  NULL
};

static const _ExtendedGDBusInterfaceInfo _org_philips_battery_mgr_interface_info =
{
  {
    -1,
    (gchar *) "org.philips.batteryMgr",
    NULL,
    (GDBusSignalInfo **) &_org_philips_battery_mgr_signal_info_pointers,
    (GDBusPropertyInfo **) &_org_philips_battery_mgr_property_info_pointers,
    (GDBusAnnotationInfo **) &_org_philips_battery_mgr_annotation_info_pointers
  },
  "org-philips-battery-mgr",
};


/**
 * org_philips_battery_mgr_interface_info:
 *
 * Gets a machine-readable description of the <link linkend="gdbus-interface-org-philips-batteryMgr.top_of_page">org.philips.batteryMgr</link> D-Bus interface.
 *
 * Returns: (transfer none): A #GDBusInterfaceInfo. Do not free.
 */
GDBusInterfaceInfo *
org_philips_battery_mgr_interface_info (void)
{
  return (GDBusInterfaceInfo *) &_org_philips_battery_mgr_interface_info.parent_struct;
}

/**
 * org_philips_battery_mgr_override_properties:
 * @klass: The class structure for a #GObject derived class.
 * @property_id_begin: The property id to assign to the first overridden property.
 *
 * Overrides all #GObject properties in the #OrgPhilipsBatteryMgr interface for a concrete class.
 * The properties are overridden in the order they are defined.
 *
 * Returns: The last property id.
 */
guint
org_philips_battery_mgr_override_properties (GObjectClass *klass, guint property_id_begin)
{
  g_object_class_override_property (klass, property_id_begin++, "qualified");
  g_object_class_override_property (klass, property_id_begin++, "present");
  g_object_class_override_property (klass, property_id_begin++, "cycle-count");
  g_object_class_override_property (klass, property_id_begin++, "status");
  g_object_class_override_property (klass, property_id_begin++, "charge-now");
  g_object_class_override_property (klass, property_id_begin++, "charge-full");
  g_object_class_override_property (klass, property_id_begin++, "charge-full-design");
  g_object_class_override_property (klass, property_id_begin++, "mode-register");
  g_object_class_override_property (klass, property_id_begin++, "status-register");
  g_object_class_override_property (klass, property_id_begin++, "capacity");
  g_object_class_override_property (klass, property_id_begin++, "absolute-charge");
  g_object_class_override_property (klass, property_id_begin++, "time-to-empty-avg");
  g_object_class_override_property (klass, property_id_begin++, "time-to-full-avg");
  g_object_class_override_property (klass, property_id_begin++, "max-error");
  g_object_class_override_property (klass, property_id_begin++, "constant-charge-current");
  g_object_class_override_property (klass, property_id_begin++, "constant-charge-voltage");
  g_object_class_override_property (klass, property_id_begin++, "temp");
  g_object_class_override_property (klass, property_id_begin++, "time-to-empty-now");
  g_object_class_override_property (klass, property_id_begin++, "current-avg");
  g_object_class_override_property (klass, property_id_begin++, "date");
  g_object_class_override_property (klass, property_id_begin++, "manufacturer");
  g_object_class_override_property (klass, property_id_begin++, "model-name");
  g_object_class_override_property (klass, property_id_begin++, "serial-number");
  return property_id_begin - 1;
}



/**
 * OrgPhilipsBatteryMgr:
 *
 * Abstract interface type for the D-Bus interface <link linkend="gdbus-interface-org-philips-batteryMgr.top_of_page">org.philips.batteryMgr</link>.
 */

/**
 * OrgPhilipsBatteryMgrIface:
 * @parent_iface: The parent interface.
 * @get_absolute_charge: Getter for the #OrgPhilipsBatteryMgr:absolute-charge property.
 * @get_capacity: Getter for the #OrgPhilipsBatteryMgr:capacity property.
 * @get_charge_full: Getter for the #OrgPhilipsBatteryMgr:charge-full property.
 * @get_charge_full_design: Getter for the #OrgPhilipsBatteryMgr:charge-full-design property.
 * @get_charge_now: Getter for the #OrgPhilipsBatteryMgr:charge-now property.
 * @get_constant_charge_current: Getter for the #OrgPhilipsBatteryMgr:constant-charge-current property.
 * @get_constant_charge_voltage: Getter for the #OrgPhilipsBatteryMgr:constant-charge-voltage property.
 * @get_current_avg: Getter for the #OrgPhilipsBatteryMgr:current-avg property.
 * @get_cycle_count: Getter for the #OrgPhilipsBatteryMgr:cycle-count property.
 * @get_date: Getter for the #OrgPhilipsBatteryMgr:date property.
 * @get_manufacturer: Getter for the #OrgPhilipsBatteryMgr:manufacturer property.
 * @get_max_error: Getter for the #OrgPhilipsBatteryMgr:max-error property.
 * @get_mode_register: Getter for the #OrgPhilipsBatteryMgr:mode-register property.
 * @get_model_name: Getter for the #OrgPhilipsBatteryMgr:model-name property.
 * @get_present: Getter for the #OrgPhilipsBatteryMgr:present property.
 * @get_qualified: Getter for the #OrgPhilipsBatteryMgr:qualified property.
 * @get_serial_number: Getter for the #OrgPhilipsBatteryMgr:serial-number property.
 * @get_status: Getter for the #OrgPhilipsBatteryMgr:status property.
 * @get_status_register: Getter for the #OrgPhilipsBatteryMgr:status-register property.
 * @get_temp: Getter for the #OrgPhilipsBatteryMgr:temp property.
 * @get_time_to_empty_avg: Getter for the #OrgPhilipsBatteryMgr:time-to-empty-avg property.
 * @get_time_to_empty_now: Getter for the #OrgPhilipsBatteryMgr:time-to-empty-now property.
 * @get_time_to_full_avg: Getter for the #OrgPhilipsBatteryMgr:time-to-full-avg property.
 * @battery_warn: Handler for the #OrgPhilipsBatteryMgr::battery-warn signal.
 *
 * Virtual table for the D-Bus interface <link linkend="gdbus-interface-org-philips-batteryMgr.top_of_page">org.philips.batteryMgr</link>.
 */

typedef OrgPhilipsBatteryMgrIface OrgPhilipsBatteryMgrInterface;
G_DEFINE_INTERFACE (OrgPhilipsBatteryMgr, org_philips_battery_mgr, G_TYPE_OBJECT)

static void
org_philips_battery_mgr_default_init (OrgPhilipsBatteryMgrIface *iface)
{
  /* GObject signals for received D-Bus signals: */
  /**
   * OrgPhilipsBatteryMgr::battery-warn:
   * @object: A #OrgPhilipsBatteryMgr.
   * @arg_type: Argument.
   *
   * On the client-side, this signal is emitted whenever the D-Bus signal <link linkend="gdbus-signal-org-philips-batteryMgr.BatteryWarn">"BatteryWarn"</link> is received.
   *
   * On the service-side, this signal can be used with e.g. g_signal_emit_by_name() to make the object emit the D-Bus signal.
   */
  g_signal_new ("battery-warn",
    G_TYPE_FROM_INTERFACE (iface),
    G_SIGNAL_RUN_LAST,
    G_STRUCT_OFFSET (OrgPhilipsBatteryMgrIface, battery_warn),
    NULL,
    NULL,
    g_cclosure_marshal_generic,
    G_TYPE_NONE,
    1, G_TYPE_UINT);

  /* GObject properties for D-Bus properties: */
  /**
   * OrgPhilipsBatteryMgr:qualified:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.qualified">"qualified"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("qualified", "qualified", "qualified", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:present:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.present">"present"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_boolean ("present", "present", "present", FALSE, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:cycle-count:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.cycle_count">"cycle_count"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("cycle-count", "cycle_count", "cycle_count", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:status:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.status">"status"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("status", "status", "status", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:charge-now:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.charge_now">"charge_now"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("charge-now", "charge_now", "charge_now", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:charge-full:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.charge_full">"charge_full"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("charge-full", "charge_full", "charge_full", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:charge-full-design:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.charge_full_design">"charge_full_design"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("charge-full-design", "charge_full_design", "charge_full_design", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:mode-register:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.mode_register">"mode_register"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("mode-register", "mode_register", "mode_register", G_MININT32, G_MAXINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:status-register:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.status_register">"status_register"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_int ("status-register", "status_register", "status_register", G_MININT32, G_MAXINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:capacity:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.capacity">"capacity"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("capacity", "capacity", "capacity", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:absolute-charge:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.absolute_charge">"absolute_charge"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("absolute-charge", "absolute_charge", "absolute_charge", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:time-to-empty-avg:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.time_to_empty_avg">"time_to_empty_avg"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("time-to-empty-avg", "time_to_empty_avg", "time_to_empty_avg", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:time-to-full-avg:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.time_to_full_avg">"time_to_full_avg"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("time-to-full-avg", "time_to_full_avg", "time_to_full_avg", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:max-error:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.max_error">"max_error"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("max-error", "max_error", "max_error", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:constant-charge-current:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.constant_charge_current">"constant_charge_current"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("constant-charge-current", "constant_charge_current", "constant_charge_current", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:constant-charge-voltage:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.constant_charge_voltage">"constant_charge_voltage"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("constant-charge-voltage", "constant_charge_voltage", "constant_charge_voltage", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:temp:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.temp">"temp"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("temp", "temp", "temp", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:time-to-empty-now:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.time_to_empty_now">"time_to_empty_now"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("time-to-empty-now", "time_to_empty_now", "time_to_empty_now", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:current-avg:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.current_avg">"current_avg"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_uint ("current-avg", "current_avg", "current_avg", 0, G_MAXUINT32, 0, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:date:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.date">"date"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("date", "date", "date", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:manufacturer:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.manufacturer">"manufacturer"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("manufacturer", "manufacturer", "manufacturer", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:model-name:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.model_name">"model_name"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("model-name", "model_name", "model_name", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
  /**
   * OrgPhilipsBatteryMgr:serial-number:
   *
   * Represents the D-Bus property <link linkend="gdbus-property-org-philips-batteryMgr.serial_number">"serial_number"</link>.
   *
   * Since the D-Bus property for this #GObject property is readable but not writable, it is meaningful to read from it on both the client- and service-side. It is only meaningful, however, to write to it on the service-side.
   */
  g_object_interface_install_property (iface,
    g_param_spec_string ("serial-number", "serial_number", "serial_number", NULL, G_PARAM_READWRITE | G_PARAM_STATIC_STRINGS));
}

/**
 * org_philips_battery_mgr_get_qualified: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.qualified">"qualified"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
org_philips_battery_mgr_get_qualified (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_qualified (object);
}

/**
 * org_philips_battery_mgr_set_qualified: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.qualified">"qualified"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_qualified (OrgPhilipsBatteryMgr *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "qualified", value, NULL);
}

/**
 * org_philips_battery_mgr_get_present: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.present">"present"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gboolean 
org_philips_battery_mgr_get_present (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_present (object);
}

/**
 * org_philips_battery_mgr_set_present: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.present">"present"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_present (OrgPhilipsBatteryMgr *object, gboolean value)
{
  g_object_set (G_OBJECT (object), "present", value, NULL);
}

/**
 * org_philips_battery_mgr_get_cycle_count: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.cycle_count">"cycle_count"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_philips_battery_mgr_get_cycle_count (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_cycle_count (object);
}

/**
 * org_philips_battery_mgr_set_cycle_count: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.cycle_count">"cycle_count"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_cycle_count (OrgPhilipsBatteryMgr *object, guint value)
{
  g_object_set (G_OBJECT (object), "cycle-count", value, NULL);
}

/**
 * org_philips_battery_mgr_get_status: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.status">"status"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_philips_battery_mgr_dup_status() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
org_philips_battery_mgr_get_status (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_status (object);
}

/**
 * org_philips_battery_mgr_dup_status: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-philips-batteryMgr.status">"status"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
org_philips_battery_mgr_dup_status (OrgPhilipsBatteryMgr *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "status", &value, NULL);
  return value;
}

/**
 * org_philips_battery_mgr_set_status: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.status">"status"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_status (OrgPhilipsBatteryMgr *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "status", value, NULL);
}

/**
 * org_philips_battery_mgr_get_charge_now: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.charge_now">"charge_now"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_philips_battery_mgr_get_charge_now (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_charge_now (object);
}

/**
 * org_philips_battery_mgr_set_charge_now: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.charge_now">"charge_now"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_charge_now (OrgPhilipsBatteryMgr *object, guint value)
{
  g_object_set (G_OBJECT (object), "charge-now", value, NULL);
}

/**
 * org_philips_battery_mgr_get_charge_full: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.charge_full">"charge_full"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_philips_battery_mgr_get_charge_full (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_charge_full (object);
}

/**
 * org_philips_battery_mgr_set_charge_full: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.charge_full">"charge_full"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_charge_full (OrgPhilipsBatteryMgr *object, guint value)
{
  g_object_set (G_OBJECT (object), "charge-full", value, NULL);
}

/**
 * org_philips_battery_mgr_get_charge_full_design: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.charge_full_design">"charge_full_design"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_philips_battery_mgr_get_charge_full_design (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_charge_full_design (object);
}

/**
 * org_philips_battery_mgr_set_charge_full_design: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.charge_full_design">"charge_full_design"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_charge_full_design (OrgPhilipsBatteryMgr *object, guint value)
{
  g_object_set (G_OBJECT (object), "charge-full-design", value, NULL);
}

/**
 * org_philips_battery_mgr_get_mode_register: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.mode_register">"mode_register"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint 
org_philips_battery_mgr_get_mode_register (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_mode_register (object);
}

/**
 * org_philips_battery_mgr_set_mode_register: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.mode_register">"mode_register"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_mode_register (OrgPhilipsBatteryMgr *object, gint value)
{
  g_object_set (G_OBJECT (object), "mode-register", value, NULL);
}

/**
 * org_philips_battery_mgr_get_status_register: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.status_register">"status_register"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
gint 
org_philips_battery_mgr_get_status_register (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_status_register (object);
}

/**
 * org_philips_battery_mgr_set_status_register: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.status_register">"status_register"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_status_register (OrgPhilipsBatteryMgr *object, gint value)
{
  g_object_set (G_OBJECT (object), "status-register", value, NULL);
}

/**
 * org_philips_battery_mgr_get_capacity: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.capacity">"capacity"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_philips_battery_mgr_get_capacity (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_capacity (object);
}

/**
 * org_philips_battery_mgr_set_capacity: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.capacity">"capacity"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_capacity (OrgPhilipsBatteryMgr *object, guint value)
{
  g_object_set (G_OBJECT (object), "capacity", value, NULL);
}

/**
 * org_philips_battery_mgr_get_absolute_charge: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.absolute_charge">"absolute_charge"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_philips_battery_mgr_get_absolute_charge (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_absolute_charge (object);
}

/**
 * org_philips_battery_mgr_set_absolute_charge: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.absolute_charge">"absolute_charge"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_absolute_charge (OrgPhilipsBatteryMgr *object, guint value)
{
  g_object_set (G_OBJECT (object), "absolute-charge", value, NULL);
}

/**
 * org_philips_battery_mgr_get_time_to_empty_avg: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.time_to_empty_avg">"time_to_empty_avg"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_philips_battery_mgr_get_time_to_empty_avg (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_time_to_empty_avg (object);
}

/**
 * org_philips_battery_mgr_set_time_to_empty_avg: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.time_to_empty_avg">"time_to_empty_avg"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_time_to_empty_avg (OrgPhilipsBatteryMgr *object, guint value)
{
  g_object_set (G_OBJECT (object), "time-to-empty-avg", value, NULL);
}

/**
 * org_philips_battery_mgr_get_time_to_full_avg: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.time_to_full_avg">"time_to_full_avg"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_philips_battery_mgr_get_time_to_full_avg (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_time_to_full_avg (object);
}

/**
 * org_philips_battery_mgr_set_time_to_full_avg: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.time_to_full_avg">"time_to_full_avg"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_time_to_full_avg (OrgPhilipsBatteryMgr *object, guint value)
{
  g_object_set (G_OBJECT (object), "time-to-full-avg", value, NULL);
}

/**
 * org_philips_battery_mgr_get_max_error: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.max_error">"max_error"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_philips_battery_mgr_get_max_error (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_max_error (object);
}

/**
 * org_philips_battery_mgr_set_max_error: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.max_error">"max_error"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_max_error (OrgPhilipsBatteryMgr *object, guint value)
{
  g_object_set (G_OBJECT (object), "max-error", value, NULL);
}

/**
 * org_philips_battery_mgr_get_constant_charge_current: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.constant_charge_current">"constant_charge_current"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_philips_battery_mgr_get_constant_charge_current (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_constant_charge_current (object);
}

/**
 * org_philips_battery_mgr_set_constant_charge_current: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.constant_charge_current">"constant_charge_current"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_constant_charge_current (OrgPhilipsBatteryMgr *object, guint value)
{
  g_object_set (G_OBJECT (object), "constant-charge-current", value, NULL);
}

/**
 * org_philips_battery_mgr_get_constant_charge_voltage: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.constant_charge_voltage">"constant_charge_voltage"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_philips_battery_mgr_get_constant_charge_voltage (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_constant_charge_voltage (object);
}

/**
 * org_philips_battery_mgr_set_constant_charge_voltage: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.constant_charge_voltage">"constant_charge_voltage"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_constant_charge_voltage (OrgPhilipsBatteryMgr *object, guint value)
{
  g_object_set (G_OBJECT (object), "constant-charge-voltage", value, NULL);
}

/**
 * org_philips_battery_mgr_get_temp: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.temp">"temp"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_philips_battery_mgr_get_temp (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_temp (object);
}

/**
 * org_philips_battery_mgr_set_temp: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.temp">"temp"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_temp (OrgPhilipsBatteryMgr *object, guint value)
{
  g_object_set (G_OBJECT (object), "temp", value, NULL);
}

/**
 * org_philips_battery_mgr_get_time_to_empty_now: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.time_to_empty_now">"time_to_empty_now"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_philips_battery_mgr_get_time_to_empty_now (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_time_to_empty_now (object);
}

/**
 * org_philips_battery_mgr_set_time_to_empty_now: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.time_to_empty_now">"time_to_empty_now"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_time_to_empty_now (OrgPhilipsBatteryMgr *object, guint value)
{
  g_object_set (G_OBJECT (object), "time-to-empty-now", value, NULL);
}

/**
 * org_philips_battery_mgr_get_current_avg: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.current_avg">"current_avg"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: The property value.
 */
guint 
org_philips_battery_mgr_get_current_avg (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_current_avg (object);
}

/**
 * org_philips_battery_mgr_set_current_avg: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.current_avg">"current_avg"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_current_avg (OrgPhilipsBatteryMgr *object, guint value)
{
  g_object_set (G_OBJECT (object), "current-avg", value, NULL);
}

/**
 * org_philips_battery_mgr_get_date: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.date">"date"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_philips_battery_mgr_dup_date() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
org_philips_battery_mgr_get_date (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_date (object);
}

/**
 * org_philips_battery_mgr_dup_date: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-philips-batteryMgr.date">"date"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
org_philips_battery_mgr_dup_date (OrgPhilipsBatteryMgr *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "date", &value, NULL);
  return value;
}

/**
 * org_philips_battery_mgr_set_date: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.date">"date"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_date (OrgPhilipsBatteryMgr *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "date", value, NULL);
}

/**
 * org_philips_battery_mgr_get_manufacturer: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.manufacturer">"manufacturer"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_philips_battery_mgr_dup_manufacturer() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
org_philips_battery_mgr_get_manufacturer (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_manufacturer (object);
}

/**
 * org_philips_battery_mgr_dup_manufacturer: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-philips-batteryMgr.manufacturer">"manufacturer"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
org_philips_battery_mgr_dup_manufacturer (OrgPhilipsBatteryMgr *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "manufacturer", &value, NULL);
  return value;
}

/**
 * org_philips_battery_mgr_set_manufacturer: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.manufacturer">"manufacturer"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_manufacturer (OrgPhilipsBatteryMgr *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "manufacturer", value, NULL);
}

/**
 * org_philips_battery_mgr_get_model_name: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.model_name">"model_name"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_philips_battery_mgr_dup_model_name() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
org_philips_battery_mgr_get_model_name (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_model_name (object);
}

/**
 * org_philips_battery_mgr_dup_model_name: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-philips-batteryMgr.model_name">"model_name"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
org_philips_battery_mgr_dup_model_name (OrgPhilipsBatteryMgr *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "model-name", &value, NULL);
  return value;
}

/**
 * org_philips_battery_mgr_set_model_name: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.model_name">"model_name"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_model_name (OrgPhilipsBatteryMgr *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "model-name", value, NULL);
}

/**
 * org_philips_battery_mgr_get_serial_number: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets the value of the <link linkend="gdbus-property-org-philips-batteryMgr.serial_number">"serial_number"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * <warning>The returned value is only valid until the property changes so on the client-side it is only safe to use this function on the thread where @object was constructed. Use org_philips_battery_mgr_dup_serial_number() if on another thread.</warning>
 *
 * Returns: (transfer none): The property value or %NULL if the property is not set. Do not free the returned value, it belongs to @object.
 */
const gchar *
org_philips_battery_mgr_get_serial_number (OrgPhilipsBatteryMgr *object)
{
  return ORG_PHILIPS_BATTERY_MGR_GET_IFACE (object)->get_serial_number (object);
}

/**
 * org_philips_battery_mgr_dup_serial_number: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 *
 * Gets a copy of the <link linkend="gdbus-property-org-philips-batteryMgr.serial_number">"serial_number"</link> D-Bus property.
 *
 * Since this D-Bus property is readable, it is meaningful to use this function on both the client- and service-side.
 *
 * Returns: (transfer full): The property value or %NULL if the property is not set. The returned value should be freed with g_free().
 */
gchar *
org_philips_battery_mgr_dup_serial_number (OrgPhilipsBatteryMgr *object)
{
  gchar *value;
  g_object_get (G_OBJECT (object), "serial-number", &value, NULL);
  return value;
}

/**
 * org_philips_battery_mgr_set_serial_number: (skip)
 * @object: A #OrgPhilipsBatteryMgr.
 * @value: The value to set.
 *
 * Sets the <link linkend="gdbus-property-org-philips-batteryMgr.serial_number">"serial_number"</link> D-Bus property to @value.
 *
 * Since this D-Bus property is not writable, it is only meaningful to use this function on the service-side.
 */
void
org_philips_battery_mgr_set_serial_number (OrgPhilipsBatteryMgr *object, const gchar *value)
{
  g_object_set (G_OBJECT (object), "serial-number", value, NULL);
}

/**
 * org_philips_battery_mgr_emit_battery_warn:
 * @object: A #OrgPhilipsBatteryMgr.
 * @arg_type: Argument to pass with the signal.
 *
 * Emits the <link linkend="gdbus-signal-org-philips-batteryMgr.BatteryWarn">"BatteryWarn"</link> D-Bus signal.
 */
void
org_philips_battery_mgr_emit_battery_warn (
    OrgPhilipsBatteryMgr *object,
    guint arg_type)
{
  g_signal_emit_by_name (object, "battery-warn", arg_type);
}

/* ------------------------------------------------------------------------ */

/**
 * OrgPhilipsBatteryMgrProxy:
 *
 * The #OrgPhilipsBatteryMgrProxy structure contains only private data and should only be accessed using the provided API.
 */

/**
 * OrgPhilipsBatteryMgrProxyClass:
 * @parent_class: The parent class.
 *
 * Class structure for #OrgPhilipsBatteryMgrProxy.
 */

struct _OrgPhilipsBatteryMgrProxyPrivate
{
  GData *qdata;
};

static void org_philips_battery_mgr_proxy_iface_init (OrgPhilipsBatteryMgrIface *iface);

#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (OrgPhilipsBatteryMgrProxy, org_philips_battery_mgr_proxy, G_TYPE_DBUS_PROXY,
                         G_ADD_PRIVATE (OrgPhilipsBatteryMgrProxy)
                         G_IMPLEMENT_INTERFACE (TYPE_ORG_PHILIPS_BATTERY_MGR, org_philips_battery_mgr_proxy_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (OrgPhilipsBatteryMgrProxy, org_philips_battery_mgr_proxy, G_TYPE_DBUS_PROXY,
                         G_IMPLEMENT_INTERFACE (TYPE_ORG_PHILIPS_BATTERY_MGR, org_philips_battery_mgr_proxy_iface_init))

#endif
static void
org_philips_battery_mgr_proxy_finalize (GObject *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  g_datalist_clear (&proxy->priv->qdata);
  G_OBJECT_CLASS (org_philips_battery_mgr_proxy_parent_class)->finalize (object);
}

static void
org_philips_battery_mgr_proxy_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 23);
  info = _org_philips_battery_mgr_property_info_pointers[prop_id - 1];
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (object), info->parent_struct.name);
  if (info->use_gvariant)
    {
      g_value_set_variant (value, variant);
    }
  else
    {
      if (variant != NULL)
        g_dbus_gvariant_to_gvalue (variant, value);
    }
  if (variant != NULL)
    g_variant_unref (variant);
}

static void
org_philips_battery_mgr_proxy_set_property_cb (GDBusProxy *proxy,
  GAsyncResult *res,
  gpointer      user_data)
{
  const _ExtendedGDBusPropertyInfo *info = user_data;
  GError *error;
  GVariant *_ret;
  error = NULL;
  _ret = g_dbus_proxy_call_finish (proxy, res, &error);
  if (!_ret)
    {
      g_warning ("Error setting property '%s' on interface org.philips.batteryMgr: %s (%s, %d)",
                 info->parent_struct.name, 
                 error->message, g_quark_to_string (error->domain), error->code);
      g_error_free (error);
    }
  else
    {
      g_variant_unref (_ret);
    }
}

static void
org_philips_battery_mgr_proxy_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  const _ExtendedGDBusPropertyInfo *info;
  GVariant *variant;
  g_assert (prop_id != 0 && prop_id - 1 < 23);
  info = _org_philips_battery_mgr_property_info_pointers[prop_id - 1];
  variant = g_dbus_gvalue_to_gvariant (value, G_VARIANT_TYPE (info->parent_struct.signature));
  g_dbus_proxy_call (G_DBUS_PROXY (object),
    "org.freedesktop.DBus.Properties.Set",
    g_variant_new ("(ssv)", "org.philips.batteryMgr", info->parent_struct.name, variant),
    G_DBUS_CALL_FLAGS_NONE,
    -1,
    NULL, (GAsyncReadyCallback) org_philips_battery_mgr_proxy_set_property_cb, (GDBusPropertyInfo *) &info->parent_struct);
  g_variant_unref (variant);
}

static void
org_philips_battery_mgr_proxy_g_signal (GDBusProxy *proxy,
  const gchar *sender_name G_GNUC_UNUSED,
  const gchar *signal_name,
  GVariant *parameters)
{
  _ExtendedGDBusSignalInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  gsize n;
  guint signal_id;
  info = (_ExtendedGDBusSignalInfo *) g_dbus_interface_info_lookup_signal ((GDBusInterfaceInfo *) &_org_philips_battery_mgr_interface_info.parent_struct, signal_name);
  if (info == NULL)
    return;
  num_params = g_variant_n_children (parameters);
  paramv = g_new0 (GValue, num_params + 1);
  g_value_init (&paramv[0], TYPE_ORG_PHILIPS_BATTERY_MGR);
  g_value_set_object (&paramv[0], proxy);
  g_variant_iter_init (&iter, parameters);
  n = 1;
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.args[n - 1];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_ORG_PHILIPS_BATTERY_MGR);
  g_signal_emitv (paramv, signal_id, 0, NULL);
  for (n = 0; n < num_params + 1; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static void
org_philips_battery_mgr_proxy_g_properties_changed (GDBusProxy *_proxy,
  GVariant *changed_properties,
  const gchar *const *invalidated_properties)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (_proxy);
  guint n;
  const gchar *key;
  GVariantIter *iter;
  _ExtendedGDBusPropertyInfo *info;
  g_variant_get (changed_properties, "a{sv}", &iter);
  while (g_variant_iter_next (iter, "{&sv}", &key, NULL))
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_org_philips_battery_mgr_interface_info.parent_struct, key);
      g_datalist_remove_data (&proxy->priv->qdata, key);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
  g_variant_iter_free (iter);
  for (n = 0; invalidated_properties[n] != NULL; n++)
    {
      info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_org_philips_battery_mgr_interface_info.parent_struct, invalidated_properties[n]);
      g_datalist_remove_data (&proxy->priv->qdata, invalidated_properties[n]);
      if (info != NULL)
        g_object_notify (G_OBJECT (proxy), info->hyphen_name);
    }
}

static gboolean 
org_philips_battery_mgr_proxy_get_qualified (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "qualified");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gboolean 
org_philips_battery_mgr_proxy_get_present (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  gboolean value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "present");
  if (variant != NULL)
    {
      value = g_variant_get_boolean (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_philips_battery_mgr_proxy_get_cycle_count (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "cycle_count");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
org_philips_battery_mgr_proxy_get_status (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "status");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_philips_battery_mgr_proxy_get_charge_now (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "charge_now");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_philips_battery_mgr_proxy_get_charge_full (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "charge_full");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_philips_battery_mgr_proxy_get_charge_full_design (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "charge_full_design");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint 
org_philips_battery_mgr_proxy_get_mode_register (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  gint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "mode_register");
  if (variant != NULL)
    {
      value = g_variant_get_int32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static gint 
org_philips_battery_mgr_proxy_get_status_register (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  gint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "status_register");
  if (variant != NULL)
    {
      value = g_variant_get_int32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_philips_battery_mgr_proxy_get_capacity (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "capacity");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_philips_battery_mgr_proxy_get_absolute_charge (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "absolute_charge");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_philips_battery_mgr_proxy_get_time_to_empty_avg (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "time_to_empty_avg");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_philips_battery_mgr_proxy_get_time_to_full_avg (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "time_to_full_avg");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_philips_battery_mgr_proxy_get_max_error (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "max_error");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_philips_battery_mgr_proxy_get_constant_charge_current (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "constant_charge_current");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_philips_battery_mgr_proxy_get_constant_charge_voltage (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "constant_charge_voltage");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_philips_battery_mgr_proxy_get_temp (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "temp");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_philips_battery_mgr_proxy_get_time_to_empty_now (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "time_to_empty_now");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static guint 
org_philips_battery_mgr_proxy_get_current_avg (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  guint value = 0;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "current_avg");
  if (variant != NULL)
    {
      value = g_variant_get_uint32 (variant);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
org_philips_battery_mgr_proxy_get_date (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "date");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
org_philips_battery_mgr_proxy_get_manufacturer (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "manufacturer");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
org_philips_battery_mgr_proxy_get_model_name (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "model_name");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static const gchar *
org_philips_battery_mgr_proxy_get_serial_number (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrProxy *proxy = ORG_PHILIPS_BATTERY_MGR_PROXY (object);
  GVariant *variant;
  const gchar *value = NULL;
  variant = g_dbus_proxy_get_cached_property (G_DBUS_PROXY (proxy), "serial_number");
  if (variant != NULL)
    {
      value = g_variant_get_string (variant, NULL);
      g_variant_unref (variant);
    }
  return value;
}

static void
org_philips_battery_mgr_proxy_init (OrgPhilipsBatteryMgrProxy *proxy)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  proxy->priv = org_philips_battery_mgr_proxy_get_instance_private (proxy);
#else
  proxy->priv = G_TYPE_INSTANCE_GET_PRIVATE (proxy, TYPE_ORG_PHILIPS_BATTERY_MGR_PROXY, OrgPhilipsBatteryMgrProxyPrivate);
#endif

  g_dbus_proxy_set_interface_info (G_DBUS_PROXY (proxy), org_philips_battery_mgr_interface_info ());
}

static void
org_philips_battery_mgr_proxy_class_init (OrgPhilipsBatteryMgrProxyClass *klass)
{
  GObjectClass *gobject_class;
  GDBusProxyClass *proxy_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize     = org_philips_battery_mgr_proxy_finalize;
  gobject_class->get_property = org_philips_battery_mgr_proxy_get_property;
  gobject_class->set_property = org_philips_battery_mgr_proxy_set_property;

  proxy_class = G_DBUS_PROXY_CLASS (klass);
  proxy_class->g_signal = org_philips_battery_mgr_proxy_g_signal;
  proxy_class->g_properties_changed = org_philips_battery_mgr_proxy_g_properties_changed;

  org_philips_battery_mgr_override_properties (gobject_class, 1);

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (OrgPhilipsBatteryMgrProxyPrivate));
#endif
}

static void
org_philips_battery_mgr_proxy_iface_init (OrgPhilipsBatteryMgrIface *iface)
{
  iface->get_qualified = org_philips_battery_mgr_proxy_get_qualified;
  iface->get_present = org_philips_battery_mgr_proxy_get_present;
  iface->get_cycle_count = org_philips_battery_mgr_proxy_get_cycle_count;
  iface->get_status = org_philips_battery_mgr_proxy_get_status;
  iface->get_charge_now = org_philips_battery_mgr_proxy_get_charge_now;
  iface->get_charge_full = org_philips_battery_mgr_proxy_get_charge_full;
  iface->get_charge_full_design = org_philips_battery_mgr_proxy_get_charge_full_design;
  iface->get_mode_register = org_philips_battery_mgr_proxy_get_mode_register;
  iface->get_status_register = org_philips_battery_mgr_proxy_get_status_register;
  iface->get_capacity = org_philips_battery_mgr_proxy_get_capacity;
  iface->get_absolute_charge = org_philips_battery_mgr_proxy_get_absolute_charge;
  iface->get_time_to_empty_avg = org_philips_battery_mgr_proxy_get_time_to_empty_avg;
  iface->get_time_to_full_avg = org_philips_battery_mgr_proxy_get_time_to_full_avg;
  iface->get_max_error = org_philips_battery_mgr_proxy_get_max_error;
  iface->get_constant_charge_current = org_philips_battery_mgr_proxy_get_constant_charge_current;
  iface->get_constant_charge_voltage = org_philips_battery_mgr_proxy_get_constant_charge_voltage;
  iface->get_temp = org_philips_battery_mgr_proxy_get_temp;
  iface->get_time_to_empty_now = org_philips_battery_mgr_proxy_get_time_to_empty_now;
  iface->get_current_avg = org_philips_battery_mgr_proxy_get_current_avg;
  iface->get_date = org_philips_battery_mgr_proxy_get_date;
  iface->get_manufacturer = org_philips_battery_mgr_proxy_get_manufacturer;
  iface->get_model_name = org_philips_battery_mgr_proxy_get_model_name;
  iface->get_serial_number = org_philips_battery_mgr_proxy_get_serial_number;
}

/**
 * org_philips_battery_mgr_proxy_new:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Asynchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-philips-batteryMgr.top_of_page">org.philips.batteryMgr</link>. See g_dbus_proxy_new() for more details.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call org_philips_battery_mgr_proxy_new_finish() to get the result of the operation.
 *
 * See org_philips_battery_mgr_proxy_new_sync() for the synchronous, blocking version of this constructor.
 */
void
org_philips_battery_mgr_proxy_new (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_ORG_PHILIPS_BATTERY_MGR_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.philips.batteryMgr", NULL);
}

/**
 * org_philips_battery_mgr_proxy_new_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_philips_battery_mgr_proxy_new().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with org_philips_battery_mgr_proxy_new().
 *
 * Returns: (transfer full) (type OrgPhilipsBatteryMgrProxy): The constructed proxy object or %NULL if @error is set.
 */
OrgPhilipsBatteryMgr *
org_philips_battery_mgr_proxy_new_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return ORG_PHILIPS_BATTERY_MGR (ret);
  else
    return NULL;
}

/**
 * org_philips_battery_mgr_proxy_new_sync:
 * @connection: A #GDBusConnection.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: (nullable): A bus name (well-known or unique) or %NULL if @connection is not a message bus connection.
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Synchronously creates a proxy for the D-Bus interface <link linkend="gdbus-interface-org-philips-batteryMgr.top_of_page">org.philips.batteryMgr</link>. See g_dbus_proxy_new_sync() for more details.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See org_philips_battery_mgr_proxy_new() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type OrgPhilipsBatteryMgrProxy): The constructed proxy object or %NULL if @error is set.
 */
OrgPhilipsBatteryMgr *
org_philips_battery_mgr_proxy_new_sync (
    GDBusConnection     *connection,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_ORG_PHILIPS_BATTERY_MGR_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-connection", connection, "g-object-path", object_path, "g-interface-name", "org.philips.batteryMgr", NULL);
  if (ret != NULL)
    return ORG_PHILIPS_BATTERY_MGR (ret);
  else
    return NULL;
}


/**
 * org_philips_battery_mgr_proxy_new_for_bus:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @callback: A #GAsyncReadyCallback to call when the request is satisfied.
 * @user_data: User data to pass to @callback.
 *
 * Like org_philips_battery_mgr_proxy_new() but takes a #GBusType instead of a #GDBusConnection.
 *
 * When the operation is finished, @callback will be invoked in the <link linkend="g-main-context-push-thread-default">thread-default main loop</link> of the thread you are calling this method from.
 * You can then call org_philips_battery_mgr_proxy_new_for_bus_finish() to get the result of the operation.
 *
 * See org_philips_battery_mgr_proxy_new_for_bus_sync() for the synchronous, blocking version of this constructor.
 */
void
org_philips_battery_mgr_proxy_new_for_bus (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GAsyncReadyCallback  callback,
    gpointer             user_data)
{
  g_async_initable_new_async (TYPE_ORG_PHILIPS_BATTERY_MGR_PROXY, G_PRIORITY_DEFAULT, cancellable, callback, user_data, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.philips.batteryMgr", NULL);
}

/**
 * org_philips_battery_mgr_proxy_new_for_bus_finish:
 * @res: The #GAsyncResult obtained from the #GAsyncReadyCallback passed to org_philips_battery_mgr_proxy_new_for_bus().
 * @error: Return location for error or %NULL
 *
 * Finishes an operation started with org_philips_battery_mgr_proxy_new_for_bus().
 *
 * Returns: (transfer full) (type OrgPhilipsBatteryMgrProxy): The constructed proxy object or %NULL if @error is set.
 */
OrgPhilipsBatteryMgr *
org_philips_battery_mgr_proxy_new_for_bus_finish (
    GAsyncResult        *res,
    GError             **error)
{
  GObject *ret;
  GObject *source_object;
  source_object = g_async_result_get_source_object (res);
  ret = g_async_initable_new_finish (G_ASYNC_INITABLE (source_object), res, error);
  g_object_unref (source_object);
  if (ret != NULL)
    return ORG_PHILIPS_BATTERY_MGR (ret);
  else
    return NULL;
}

/**
 * org_philips_battery_mgr_proxy_new_for_bus_sync:
 * @bus_type: A #GBusType.
 * @flags: Flags from the #GDBusProxyFlags enumeration.
 * @name: A bus name (well-known or unique).
 * @object_path: An object path.
 * @cancellable: (nullable): A #GCancellable or %NULL.
 * @error: Return location for error or %NULL
 *
 * Like org_philips_battery_mgr_proxy_new_sync() but takes a #GBusType instead of a #GDBusConnection.
 *
 * The calling thread is blocked until a reply is received.
 *
 * See org_philips_battery_mgr_proxy_new_for_bus() for the asynchronous version of this constructor.
 *
 * Returns: (transfer full) (type OrgPhilipsBatteryMgrProxy): The constructed proxy object or %NULL if @error is set.
 */
OrgPhilipsBatteryMgr *
org_philips_battery_mgr_proxy_new_for_bus_sync (
    GBusType             bus_type,
    GDBusProxyFlags      flags,
    const gchar         *name,
    const gchar         *object_path,
    GCancellable        *cancellable,
    GError             **error)
{
  GInitable *ret;
  ret = g_initable_new (TYPE_ORG_PHILIPS_BATTERY_MGR_PROXY, cancellable, error, "g-flags", flags, "g-name", name, "g-bus-type", bus_type, "g-object-path", object_path, "g-interface-name", "org.philips.batteryMgr", NULL);
  if (ret != NULL)
    return ORG_PHILIPS_BATTERY_MGR (ret);
  else
    return NULL;
}


/* ------------------------------------------------------------------------ */

/**
 * OrgPhilipsBatteryMgrSkeleton:
 *
 * The #OrgPhilipsBatteryMgrSkeleton structure contains only private data and should only be accessed using the provided API.
 */

/**
 * OrgPhilipsBatteryMgrSkeletonClass:
 * @parent_class: The parent class.
 *
 * Class structure for #OrgPhilipsBatteryMgrSkeleton.
 */

struct _OrgPhilipsBatteryMgrSkeletonPrivate
{
  GValue *properties;
  GList *changed_properties;
  GSource *changed_properties_idle_source;
  GMainContext *context;
  GMutex lock;
};

static void
_org_philips_battery_mgr_skeleton_handle_method_call (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name,
  const gchar *method_name,
  GVariant *parameters,
  GDBusMethodInvocation *invocation,
  gpointer user_data)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (user_data);
  _ExtendedGDBusMethodInfo *info;
  GVariantIter iter;
  GVariant *child;
  GValue *paramv;
  gsize num_params;
  guint num_extra;
  gsize n;
  guint signal_id;
  GValue return_value = G_VALUE_INIT;
  info = (_ExtendedGDBusMethodInfo *) g_dbus_method_invocation_get_method_info (invocation);
  g_assert (info != NULL);
  num_params = g_variant_n_children (parameters);
  num_extra = info->pass_fdlist ? 3 : 2;  paramv = g_new0 (GValue, num_params + num_extra);
  n = 0;
  g_value_init (&paramv[n], TYPE_ORG_PHILIPS_BATTERY_MGR);
  g_value_set_object (&paramv[n++], skeleton);
  g_value_init (&paramv[n], G_TYPE_DBUS_METHOD_INVOCATION);
  g_value_set_object (&paramv[n++], invocation);
  if (info->pass_fdlist)
    {
#ifdef G_OS_UNIX
      g_value_init (&paramv[n], G_TYPE_UNIX_FD_LIST);
      g_value_set_object (&paramv[n++], g_dbus_message_get_unix_fd_list (g_dbus_method_invocation_get_message (invocation)));
#else
      g_assert_not_reached ();
#endif
    }
  g_variant_iter_init (&iter, parameters);
  while ((child = g_variant_iter_next_value (&iter)) != NULL)
    {
      _ExtendedGDBusArgInfo *arg_info = (_ExtendedGDBusArgInfo *) info->parent_struct.in_args[n - num_extra];
      if (arg_info->use_gvariant)
        {
          g_value_init (&paramv[n], G_TYPE_VARIANT);
          g_value_set_variant (&paramv[n], child);
          n++;
        }
      else
        g_dbus_gvariant_to_gvalue (child, &paramv[n++]);
      g_variant_unref (child);
    }
  signal_id = g_signal_lookup (info->signal_name, TYPE_ORG_PHILIPS_BATTERY_MGR);
  g_value_init (&return_value, G_TYPE_BOOLEAN);
  g_signal_emitv (paramv, signal_id, 0, &return_value);
  if (!g_value_get_boolean (&return_value))
    g_dbus_method_invocation_return_error (invocation, G_DBUS_ERROR, G_DBUS_ERROR_UNKNOWN_METHOD, "Method %s is not implemented on interface %s", method_name, interface_name);
  g_value_unset (&return_value);
  for (n = 0; n < num_params + num_extra; n++)
    g_value_unset (&paramv[n]);
  g_free (paramv);
}

static GVariant *
_org_philips_battery_mgr_skeleton_handle_get_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GError **error,
  gpointer user_data)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  GVariant *ret;
  ret = NULL;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_org_philips_battery_mgr_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      g_value_init (&value, pspec->value_type);
      g_object_get_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      ret = g_dbus_gvalue_to_gvariant (&value, G_VARIANT_TYPE (info->parent_struct.signature));
      g_value_unset (&value);
    }
  return ret;
}

static gboolean
_org_philips_battery_mgr_skeleton_handle_set_property (
  GDBusConnection *connection G_GNUC_UNUSED,
  const gchar *sender G_GNUC_UNUSED,
  const gchar *object_path G_GNUC_UNUSED,
  const gchar *interface_name G_GNUC_UNUSED,
  const gchar *property_name,
  GVariant *variant,
  GError **error,
  gpointer user_data)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (user_data);
  GValue value = G_VALUE_INIT;
  GParamSpec *pspec;
  _ExtendedGDBusPropertyInfo *info;
  gboolean ret;
  ret = FALSE;
  info = (_ExtendedGDBusPropertyInfo *) g_dbus_interface_info_lookup_property ((GDBusInterfaceInfo *) &_org_philips_battery_mgr_interface_info.parent_struct, property_name);
  g_assert (info != NULL);
  pspec = g_object_class_find_property (G_OBJECT_GET_CLASS (skeleton), info->hyphen_name);
  if (pspec == NULL)
    {
      g_set_error (error, G_DBUS_ERROR, G_DBUS_ERROR_INVALID_ARGS, "No property with name %s", property_name);
    }
  else
    {
      if (info->use_gvariant)
        g_value_set_variant (&value, variant);
      else
        g_dbus_gvariant_to_gvalue (variant, &value);
      g_object_set_property (G_OBJECT (skeleton), info->hyphen_name, &value);
      g_value_unset (&value);
      ret = TRUE;
    }
  return ret;
}

static const GDBusInterfaceVTable _org_philips_battery_mgr_skeleton_vtable =
{
  _org_philips_battery_mgr_skeleton_handle_method_call,
  _org_philips_battery_mgr_skeleton_handle_get_property,
  _org_philips_battery_mgr_skeleton_handle_set_property,
  {NULL}
};

static GDBusInterfaceInfo *
org_philips_battery_mgr_skeleton_dbus_interface_get_info (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return org_philips_battery_mgr_interface_info ();
}

static GDBusInterfaceVTable *
org_philips_battery_mgr_skeleton_dbus_interface_get_vtable (GDBusInterfaceSkeleton *skeleton G_GNUC_UNUSED)
{
  return (GDBusInterfaceVTable *) &_org_philips_battery_mgr_skeleton_vtable;
}

static GVariant *
org_philips_battery_mgr_skeleton_dbus_interface_get_properties (GDBusInterfaceSkeleton *_skeleton)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (_skeleton);

  GVariantBuilder builder;
  guint n;
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  if (_org_philips_battery_mgr_interface_info.parent_struct.properties == NULL)
    goto out;
  for (n = 0; _org_philips_battery_mgr_interface_info.parent_struct.properties[n] != NULL; n++)
    {
      GDBusPropertyInfo *info = _org_philips_battery_mgr_interface_info.parent_struct.properties[n];
      if (info->flags & G_DBUS_PROPERTY_INFO_FLAGS_READABLE)
        {
          GVariant *value;
          value = _org_philips_battery_mgr_skeleton_handle_get_property (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)), NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.philips.batteryMgr", info->name, NULL, skeleton);
          if (value != NULL)
            {
              g_variant_take_ref (value);
              g_variant_builder_add (&builder, "{sv}", info->name, value);
              g_variant_unref (value);
            }
        }
    }
out:
  return g_variant_builder_end (&builder);
}

static gboolean _org_philips_battery_mgr_emit_changed (gpointer user_data);

static void
org_philips_battery_mgr_skeleton_dbus_interface_flush (GDBusInterfaceSkeleton *_skeleton)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (_skeleton);
  gboolean emit_changed = FALSE;

  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    {
      g_source_destroy (skeleton->priv->changed_properties_idle_source);
      skeleton->priv->changed_properties_idle_source = NULL;
      emit_changed = TRUE;
    }
  g_mutex_unlock (&skeleton->priv->lock);

  if (emit_changed)
    _org_philips_battery_mgr_emit_changed (skeleton);
}

static void
_org_philips_battery_mgr_on_signal_battery_warn (
    OrgPhilipsBatteryMgr *object,
    guint arg_type)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);

  GList      *connections, *l;
  GVariant   *signal_variant;
  connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));

  signal_variant = g_variant_ref_sink (g_variant_new ("(u)",
                   arg_type));
  for (l = connections; l != NULL; l = l->next)
    {
      GDBusConnection *connection = l->data;
      g_dbus_connection_emit_signal (connection,
        NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)), "org.philips.batteryMgr", "BatteryWarn",
        signal_variant, NULL);
    }
  g_variant_unref (signal_variant);
  g_list_free_full (connections, g_object_unref);
}

static void org_philips_battery_mgr_skeleton_iface_init (OrgPhilipsBatteryMgrIface *iface);
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
G_DEFINE_TYPE_WITH_CODE (OrgPhilipsBatteryMgrSkeleton, org_philips_battery_mgr_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_ADD_PRIVATE (OrgPhilipsBatteryMgrSkeleton)
                         G_IMPLEMENT_INTERFACE (TYPE_ORG_PHILIPS_BATTERY_MGR, org_philips_battery_mgr_skeleton_iface_init))

#else
G_DEFINE_TYPE_WITH_CODE (OrgPhilipsBatteryMgrSkeleton, org_philips_battery_mgr_skeleton, G_TYPE_DBUS_INTERFACE_SKELETON,
                         G_IMPLEMENT_INTERFACE (TYPE_ORG_PHILIPS_BATTERY_MGR, org_philips_battery_mgr_skeleton_iface_init))

#endif
static void
org_philips_battery_mgr_skeleton_finalize (GObject *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  guint n;
  for (n = 0; n < 23; n++)
    g_value_unset (&skeleton->priv->properties[n]);
  g_free (skeleton->priv->properties);
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  if (skeleton->priv->changed_properties_idle_source != NULL)
    g_source_destroy (skeleton->priv->changed_properties_idle_source);
  g_main_context_unref (skeleton->priv->context);
  g_mutex_clear (&skeleton->priv->lock);
  G_OBJECT_CLASS (org_philips_battery_mgr_skeleton_parent_class)->finalize (object);
}

static void
org_philips_battery_mgr_skeleton_get_property (GObject      *object,
  guint         prop_id,
  GValue       *value,
  GParamSpec   *pspec G_GNUC_UNUSED)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 23);
  g_mutex_lock (&skeleton->priv->lock);
  g_value_copy (&skeleton->priv->properties[prop_id - 1], value);
  g_mutex_unlock (&skeleton->priv->lock);
}

static gboolean
_org_philips_battery_mgr_emit_changed (gpointer user_data)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (user_data);
  GList *l;
  GVariantBuilder builder;
  GVariantBuilder invalidated_builder;
  guint num_changes;

  g_mutex_lock (&skeleton->priv->lock);
  g_variant_builder_init (&builder, G_VARIANT_TYPE ("a{sv}"));
  g_variant_builder_init (&invalidated_builder, G_VARIANT_TYPE ("as"));
  for (l = skeleton->priv->changed_properties, num_changes = 0; l != NULL; l = l->next)
    {
      ChangedProperty *cp = l->data;
      GVariant *variant;
      const GValue *cur_value;

      cur_value = &skeleton->priv->properties[cp->prop_id - 1];
      if (!_g_value_equal (cur_value, &cp->orig_value))
        {
          variant = g_dbus_gvalue_to_gvariant (cur_value, G_VARIANT_TYPE (cp->info->parent_struct.signature));
          g_variant_builder_add (&builder, "{sv}", cp->info->parent_struct.name, variant);
          g_variant_unref (variant);
          num_changes++;
        }
    }
  if (num_changes > 0)
    {
      GList *connections, *ll;
      GVariant *signal_variant;
      signal_variant = g_variant_ref_sink (g_variant_new ("(sa{sv}as)", "org.philips.batteryMgr",
                                           &builder, &invalidated_builder));
      connections = g_dbus_interface_skeleton_get_connections (G_DBUS_INTERFACE_SKELETON (skeleton));
      for (ll = connections; ll != NULL; ll = ll->next)
        {
          GDBusConnection *connection = ll->data;

          g_dbus_connection_emit_signal (connection,
                                         NULL, g_dbus_interface_skeleton_get_object_path (G_DBUS_INTERFACE_SKELETON (skeleton)),
                                         "org.freedesktop.DBus.Properties",
                                         "PropertiesChanged",
                                         signal_variant,
                                         NULL);
        }
      g_variant_unref (signal_variant);
      g_list_free_full (connections, g_object_unref);
    }
  else
    {
      g_variant_builder_clear (&builder);
      g_variant_builder_clear (&invalidated_builder);
    }
  g_list_free_full (skeleton->priv->changed_properties, (GDestroyNotify) _changed_property_free);
  skeleton->priv->changed_properties = NULL;
  skeleton->priv->changed_properties_idle_source = NULL;
  g_mutex_unlock (&skeleton->priv->lock);
  return FALSE;
}

static void
_org_philips_battery_mgr_schedule_emit_changed (OrgPhilipsBatteryMgrSkeleton *skeleton, const _ExtendedGDBusPropertyInfo *info, guint prop_id, const GValue *orig_value)
{
  ChangedProperty *cp;
  GList *l;
  cp = NULL;
  for (l = skeleton->priv->changed_properties; l != NULL; l = l->next)
    {
      ChangedProperty *i_cp = l->data;
      if (i_cp->info == info)
        {
          cp = i_cp;
          break;
        }
    }
  if (cp == NULL)
    {
      cp = g_new0 (ChangedProperty, 1);
      cp->prop_id = prop_id;
      cp->info = info;
      skeleton->priv->changed_properties = g_list_prepend (skeleton->priv->changed_properties, cp);
      g_value_init (&cp->orig_value, G_VALUE_TYPE (orig_value));
      g_value_copy (orig_value, &cp->orig_value);
    }
}

static void
org_philips_battery_mgr_skeleton_notify (GObject      *object,
  GParamSpec *pspec G_GNUC_UNUSED)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  g_mutex_lock (&skeleton->priv->lock);
  if (skeleton->priv->changed_properties != NULL &&
      skeleton->priv->changed_properties_idle_source == NULL)
    {
      skeleton->priv->changed_properties_idle_source = g_idle_source_new ();
      g_source_set_priority (skeleton->priv->changed_properties_idle_source, G_PRIORITY_DEFAULT);
      g_source_set_callback (skeleton->priv->changed_properties_idle_source, _org_philips_battery_mgr_emit_changed, g_object_ref (skeleton), (GDestroyNotify) g_object_unref);
      g_source_set_name (skeleton->priv->changed_properties_idle_source, "[generated] _org_philips_battery_mgr_emit_changed");
      g_source_attach (skeleton->priv->changed_properties_idle_source, skeleton->priv->context);
      g_source_unref (skeleton->priv->changed_properties_idle_source);
    }
  g_mutex_unlock (&skeleton->priv->lock);
}

static void
org_philips_battery_mgr_skeleton_set_property (GObject      *object,
  guint         prop_id,
  const GValue *value,
  GParamSpec   *pspec)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  g_assert (prop_id != 0 && prop_id - 1 < 23);
  g_mutex_lock (&skeleton->priv->lock);
  g_object_freeze_notify (object);
  if (!_g_value_equal (value, &skeleton->priv->properties[prop_id - 1]))
    {
      if (g_dbus_interface_skeleton_get_connection (G_DBUS_INTERFACE_SKELETON (skeleton)) != NULL)
        _org_philips_battery_mgr_schedule_emit_changed (skeleton, _org_philips_battery_mgr_property_info_pointers[prop_id - 1], prop_id, &skeleton->priv->properties[prop_id - 1]);
      g_value_copy (value, &skeleton->priv->properties[prop_id - 1]);
      g_object_notify_by_pspec (object, pspec);
    }
  g_mutex_unlock (&skeleton->priv->lock);
  g_object_thaw_notify (object);
}

static void
org_philips_battery_mgr_skeleton_init (OrgPhilipsBatteryMgrSkeleton *skeleton)
{
#if GLIB_VERSION_MAX_ALLOWED >= GLIB_VERSION_2_38
  skeleton->priv = org_philips_battery_mgr_skeleton_get_instance_private (skeleton);
#else
  skeleton->priv = G_TYPE_INSTANCE_GET_PRIVATE (skeleton, TYPE_ORG_PHILIPS_BATTERY_MGR_SKELETON, OrgPhilipsBatteryMgrSkeletonPrivate);
#endif

  g_mutex_init (&skeleton->priv->lock);
  skeleton->priv->context = g_main_context_ref_thread_default ();
  skeleton->priv->properties = g_new0 (GValue, 23);
  g_value_init (&skeleton->priv->properties[0], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[1], G_TYPE_BOOLEAN);
  g_value_init (&skeleton->priv->properties[2], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[3], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[4], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[5], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[6], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[7], G_TYPE_INT);
  g_value_init (&skeleton->priv->properties[8], G_TYPE_INT);
  g_value_init (&skeleton->priv->properties[9], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[10], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[11], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[12], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[13], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[14], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[15], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[16], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[17], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[18], G_TYPE_UINT);
  g_value_init (&skeleton->priv->properties[19], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[20], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[21], G_TYPE_STRING);
  g_value_init (&skeleton->priv->properties[22], G_TYPE_STRING);
}

static gboolean 
org_philips_battery_mgr_skeleton_get_qualified (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[0]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gboolean 
org_philips_battery_mgr_skeleton_get_present (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  gboolean value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_boolean (&(skeleton->priv->properties[1]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_philips_battery_mgr_skeleton_get_cycle_count (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[2]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
org_philips_battery_mgr_skeleton_get_status (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[3]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_philips_battery_mgr_skeleton_get_charge_now (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[4]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_philips_battery_mgr_skeleton_get_charge_full (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[5]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_philips_battery_mgr_skeleton_get_charge_full_design (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[6]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint 
org_philips_battery_mgr_skeleton_get_mode_register (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[7]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static gint 
org_philips_battery_mgr_skeleton_get_status_register (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  gint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_int (&(skeleton->priv->properties[8]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_philips_battery_mgr_skeleton_get_capacity (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[9]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_philips_battery_mgr_skeleton_get_absolute_charge (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[10]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_philips_battery_mgr_skeleton_get_time_to_empty_avg (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[11]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_philips_battery_mgr_skeleton_get_time_to_full_avg (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[12]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_philips_battery_mgr_skeleton_get_max_error (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[13]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_philips_battery_mgr_skeleton_get_constant_charge_current (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[14]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_philips_battery_mgr_skeleton_get_constant_charge_voltage (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[15]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_philips_battery_mgr_skeleton_get_temp (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[16]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_philips_battery_mgr_skeleton_get_time_to_empty_now (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[17]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static guint 
org_philips_battery_mgr_skeleton_get_current_avg (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  guint value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_uint (&(skeleton->priv->properties[18]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
org_philips_battery_mgr_skeleton_get_date (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[19]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
org_philips_battery_mgr_skeleton_get_manufacturer (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[20]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
org_philips_battery_mgr_skeleton_get_model_name (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[21]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static const gchar *
org_philips_battery_mgr_skeleton_get_serial_number (OrgPhilipsBatteryMgr *object)
{
  OrgPhilipsBatteryMgrSkeleton *skeleton = ORG_PHILIPS_BATTERY_MGR_SKELETON (object);
  const gchar *value;
  g_mutex_lock (&skeleton->priv->lock);
  value = g_value_get_string (&(skeleton->priv->properties[22]));
  g_mutex_unlock (&skeleton->priv->lock);
  return value;
}

static void
org_philips_battery_mgr_skeleton_class_init (OrgPhilipsBatteryMgrSkeletonClass *klass)
{
  GObjectClass *gobject_class;
  GDBusInterfaceSkeletonClass *skeleton_class;

  gobject_class = G_OBJECT_CLASS (klass);
  gobject_class->finalize = org_philips_battery_mgr_skeleton_finalize;
  gobject_class->get_property = org_philips_battery_mgr_skeleton_get_property;
  gobject_class->set_property = org_philips_battery_mgr_skeleton_set_property;
  gobject_class->notify       = org_philips_battery_mgr_skeleton_notify;


  org_philips_battery_mgr_override_properties (gobject_class, 1);

  skeleton_class = G_DBUS_INTERFACE_SKELETON_CLASS (klass);
  skeleton_class->get_info = org_philips_battery_mgr_skeleton_dbus_interface_get_info;
  skeleton_class->get_properties = org_philips_battery_mgr_skeleton_dbus_interface_get_properties;
  skeleton_class->flush = org_philips_battery_mgr_skeleton_dbus_interface_flush;
  skeleton_class->get_vtable = org_philips_battery_mgr_skeleton_dbus_interface_get_vtable;

#if GLIB_VERSION_MAX_ALLOWED < GLIB_VERSION_2_38
  g_type_class_add_private (klass, sizeof (OrgPhilipsBatteryMgrSkeletonPrivate));
#endif
}

static void
org_philips_battery_mgr_skeleton_iface_init (OrgPhilipsBatteryMgrIface *iface)
{
  iface->battery_warn = _org_philips_battery_mgr_on_signal_battery_warn;
  iface->get_qualified = org_philips_battery_mgr_skeleton_get_qualified;
  iface->get_present = org_philips_battery_mgr_skeleton_get_present;
  iface->get_cycle_count = org_philips_battery_mgr_skeleton_get_cycle_count;
  iface->get_status = org_philips_battery_mgr_skeleton_get_status;
  iface->get_charge_now = org_philips_battery_mgr_skeleton_get_charge_now;
  iface->get_charge_full = org_philips_battery_mgr_skeleton_get_charge_full;
  iface->get_charge_full_design = org_philips_battery_mgr_skeleton_get_charge_full_design;
  iface->get_mode_register = org_philips_battery_mgr_skeleton_get_mode_register;
  iface->get_status_register = org_philips_battery_mgr_skeleton_get_status_register;
  iface->get_capacity = org_philips_battery_mgr_skeleton_get_capacity;
  iface->get_absolute_charge = org_philips_battery_mgr_skeleton_get_absolute_charge;
  iface->get_time_to_empty_avg = org_philips_battery_mgr_skeleton_get_time_to_empty_avg;
  iface->get_time_to_full_avg = org_philips_battery_mgr_skeleton_get_time_to_full_avg;
  iface->get_max_error = org_philips_battery_mgr_skeleton_get_max_error;
  iface->get_constant_charge_current = org_philips_battery_mgr_skeleton_get_constant_charge_current;
  iface->get_constant_charge_voltage = org_philips_battery_mgr_skeleton_get_constant_charge_voltage;
  iface->get_temp = org_philips_battery_mgr_skeleton_get_temp;
  iface->get_time_to_empty_now = org_philips_battery_mgr_skeleton_get_time_to_empty_now;
  iface->get_current_avg = org_philips_battery_mgr_skeleton_get_current_avg;
  iface->get_date = org_philips_battery_mgr_skeleton_get_date;
  iface->get_manufacturer = org_philips_battery_mgr_skeleton_get_manufacturer;
  iface->get_model_name = org_philips_battery_mgr_skeleton_get_model_name;
  iface->get_serial_number = org_philips_battery_mgr_skeleton_get_serial_number;
}

/**
 * org_philips_battery_mgr_skeleton_new:
 *
 * Creates a skeleton object for the D-Bus interface <link linkend="gdbus-interface-org-philips-batteryMgr.top_of_page">org.philips.batteryMgr</link>.
 *
 * Returns: (transfer full) (type OrgPhilipsBatteryMgrSkeleton): The skeleton object.
 */
OrgPhilipsBatteryMgr *
org_philips_battery_mgr_skeleton_new (void)
{
  return ORG_PHILIPS_BATTERY_MGR (g_object_new (TYPE_ORG_PHILIPS_BATTERY_MGR_SKELETON, NULL));
}

